device = serialport("COM3", 115200);
device.Timeout = 10;
out = read(device, 999999, 'uint8');
temp = out(1:4);
temp = typecast(uint8(temp), 'int32');
temp = temp / 4;
f = fopen("data/mb1/test" +  tem.bin", "w");
fwrite(f, out(4:size(out,2)));
fclose(f);